
function Utils(){
	this.getObjectByID = function (arr, id){
		var i, num_elements = arr.length;
		for (i = 0; i < num_elements; i ++){
			if (arr[i].id == id)
				return arr[i];
		}
		return null;
		
	}
};